<div id="<%= dom_id(comment) %>">
  <div class="flex min-w-full">
    <div class="px-10 min-w-full my-5">
      <div class="bg-white max-w-xl rounded-2xl px-10 py-8 shadow-lg hover:shadow-2xl transition duration-500 min-w-full">
        <div class="mt-4">
          <p class="mt-4 text-md text-gray-600"><%= comment.content %></p>
          <div class="flex justify-between items-center">
            <div class="mt-4 flex items-center space-x-4 py-6">
              <div class="text-sm font-semibold">User • <span class="font-normal"> <%= time_ago_in_words(comment.created_at) %> </span></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
